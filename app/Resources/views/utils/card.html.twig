<div class="card">
    <div class="card-block card-inverse card-{{style| default('primary')}} text-center">
        <h4 class="card-title">{{title}}</h4>
</div>
    <div class="card-block">
        {% block content %}

        {% endblock %}
    </div>
</div>