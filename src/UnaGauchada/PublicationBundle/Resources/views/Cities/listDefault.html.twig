{% for department in departments %}
    <option value="{{ department.id }}" {% if publication.department == department %} selected="selected" {% endif %}>{{ department.province.name | title }}{% if department.province.name != '' %}, {% endif %}{{ department.name | title }}</option>
{% endfor %}