{% macro item(title, img, route) %}

    <div class="item  col-xs-4 col-lg-4">
        <div class="thumbnail">
            <img class="group list-group-image" src="{{ asset(img) }}" alt="" />
            <div class="caption">
                <h4 class="group inner list-group-item-heading">
                    {{ title }}</h4>
                <div class="row">
                    <div class="col-xs-12 col-md-6">
                        <a class="btn btn-success" href="{{ route }}">Descripción</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endmacro %}