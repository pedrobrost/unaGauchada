{% macro message(msg,id) %}

<li class="list-group-item no-results" id="{{id}}">
    <div class="card">
        <h2>No tienes Postulaciones {{msg}}</h2>
        <br>
        <i class="fa fa-users aria-hidden="true"></i>
        <br>
    </div>
</li>

{% endmacro %}