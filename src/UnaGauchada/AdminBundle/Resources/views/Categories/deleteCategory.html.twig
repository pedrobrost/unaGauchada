<div class="modal fade" id="delete{{ category.id }}" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <form class="addCategoryForm" method="" action="">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Eliminar categoria</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
                </div>
                <div class="modal-body">
                    <p>"Esta a punto de eliminar la categoría {{ category.name }}, ¿Está seguro que desea continuar?"</p>
                </div>
                <div class="modal-footer">
                    <input type="hidden" name="deletedId" value="{{ category.id }}">
                    <button type="submit" class="btn btn-secondary">Eliminar</button>
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Cancelar</button>
                </div>
            </form>
        </div>
    </div>
</div>